import React from "react";
import ReactDOM from "react-dom/client";
import RinconMenu from "./RinconMenu.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RinconMenu />
  </React.StrictMode>
);
